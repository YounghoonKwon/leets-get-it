class Solution:
    def isPalindrome(self, x: int) -> bool:
        if x < 0:
            return 'false'
        
        reversed = str(x)[::-1]
        
        if reversed == str(x):
            return 'true'
        else:
            return 'false'
        
